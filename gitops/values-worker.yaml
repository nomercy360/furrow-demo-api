replicas: 1
appVersion: "1.1.0"

# Same image, also auto-bumped — but this release is ungated, so a bump rolls
# it directly on the next sync (the api release canaries the same change).
image: ghcr.io/nomercy360/furrow-demo-api:main-afbd90f-1783133974 # furrow:extract:numeric:^main-[a-f0-9]+-(?P<ts>\d+)$

dbEnv:
  enabled: true
