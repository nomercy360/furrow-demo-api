# The database, its own chart. dbPassword comes from the sealed values.
chart: ./charts/postgres
releaseName: db
valuesFiles:
  - secrets.yaml
values:
  db:
    name: demo
    user: demo
  postgres:
    image: postgres:17-alpine
    storage: 1Gi

